def reverse_string(x):
    for x in range(x):
        for x2 in range(x):
            if x(x2) <= x(x):
                (x2), (x) = x(x), x(x2)
        return x


word = "dad"
print(reverse_string(word))


def reverse_string2(string):
    return string[::-1]


word = "Hello"
print(word)
